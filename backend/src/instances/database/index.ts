/**
 * @summary Database instance configuration
 * @module instances/database
 *
 * @description
 * Manages database connection pool and provides singleton access.
 */

import { getPool } from '@/utils/database';

export { getPool };
