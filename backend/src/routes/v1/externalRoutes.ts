/**
 * @summary External (public) API routes configuration
 * @module routes/v1/externalRoutes
 *
 * @description
 * Configures all public API endpoints that do not require authentication.
 * Includes security, subscription, and public resource routes.
 */

import { Router } from 'express';

const router = Router();

/**
 * @remarks
 * External routes will be added here as features are implemented.
 * Examples:
 * - /security/login
 * - /security/register
 * - /public/courses (public course catalog)
 */

export default router;
